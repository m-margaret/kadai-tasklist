<h1>Task一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task_path(task) %>：<%= task.content %>&gt;<%= task.status %></li>
  <% end %>
</ul>

<%= link_to '新規Taskの投稿', new_task_path %>